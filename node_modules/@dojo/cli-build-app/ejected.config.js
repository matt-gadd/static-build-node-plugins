"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const dev_config_1 = require("./dev.config");
const dist_config_1 = require("./dist.config");
const unit_config_1 = require("./unit.config");
const functional_config_1 = require("./functional.config");
function webpackConfig(env = {}) {
    const { mode = 'dist' } = env;
    const rc = require('./build-options.json');
    let config;
    if (mode === 'dev') {
        config = dev_config_1.default(rc);
    }
    else if (mode === 'unit') {
        config = unit_config_1.default(rc);
    }
    else if (mode === 'functional') {
        config = functional_config_1.default(rc);
    }
    else {
        config = dist_config_1.default(rc);
    }
    return config;
}
module.exports = webpackConfig;
//# sourceMappingURL=ejected.config.js.map